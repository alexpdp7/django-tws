#from django.contrib.auth import models as auth_models
#from django_tws import users
#from {{ project }}.dj.{{ app }} import models

# The Python code below executes after creating a blank database in devserver.
# Create here test data for your application.

# If this is the first app you create, then creating some Django users can be convenient:
#
# admin = users.user_manager().create_superuser("admin", password="admin")
# staff = users.user_manager().create_user("staff", password="staff", is_staff=True)
# user = users.user_manager().create_user("user", password="user")
#
# group = auth_models.Group(name="group")
# group.save()
#
# for permission in [
#     "add_xxx",
#     "change_xxx",
#     "delete_xxx",
#     "view_xxx",
# ]:
#     permission = auth_models.Permission.objects.get(codename=permission)
#     group.permissions.add(permission)
#
# group.user_set.add(staff)

# For all apps, you might want to create instances of the models in the app:
#
# models.XxxModel(
#     field=...,
#     field=...,
# ).save()
